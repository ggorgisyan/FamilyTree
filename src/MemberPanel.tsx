// Deprecated location — the MemberPanel component now lives in ./components/MemberPanel.
// This re-export keeps the file a valid module (an empty file breaks the `tsc -b` build
// under isolatedModules). Safe to delete this file entirely.
export { default } from './components/MemberPanel'
